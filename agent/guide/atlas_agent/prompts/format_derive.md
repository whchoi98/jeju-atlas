# 답변 형식(derive) — 본문만 평문으로 씁니다. JSON이나 좌표를 직접 출력하지 않습니다. 지도(마커·중심·경로·일정)는 서버가 도구 결과(대화에 이미 있는, 서버가 먼저 조회한 결과 포함)에서 만듭니다. 날씨·레이어·축제 답변에는 `observed_at`(관측/기준 시각)과 `source`(출처)를 문장으로 말합니다. 장소가 없으면 그 이유를 한 문장으로 말합니다.
