# 보안 안내

## 보고 방법

키, 토큰, 대화 내용이나 개인 정보가 포함된 재현 자료는 공개 이슈에 올리지 않습니다.
GitHub의 비공개 취약점 신고 기능이 활성화되어 있으면 해당 경로를 사용합니다.
그 경로를 사용할 수 없다면 저장소 소유자에게 비공개 전달 방법을 먼저 확인합니다.
일반 이슈에는 민감 내용을 제거한 증상과 영향을 적습니다.

## 비밀값 관리

운영의 키와 서명값은 IAM 역할, Secrets Manager와 SSM Parameter Store 경계에서 관리합니다.
브라우저 번들, 문서, 프롬프트와 Git 이력에 값을 넣지 않습니다.
`.gitignore`의 제외 규칙을 `git add -f`로 우회하지 않습니다.
계정 ID, 리소스 ARN과 모델 ID는 비밀값과 다르지만 배포 대상과 권한 범위는 따로 확인합니다.

Gitleaks 기본 규칙을 사용합니다.
`.gitleaks.toml`의 예외는 공개 localStorage 키와 합성 테스트 값의 정확한 파일/문자열 조합에만 적용합니다.
테스트 폴더 전체나 과거 커밋 전체를 제외하지 않습니다.

```bash
gitleaks git --config=.gitleaks.toml --log-opts="--all --full-history" --redact=100 .
```

## 서비스 경계

CloudFront와 ALB 사이에는 원본 검증을 적용하고 Fargate에는 Public IP를 할당하지 않습니다.
웹 요청의 서명 세션과 CSRF 검사, AWS 서비스 간 IAM 인증을 사용합니다.
AI 이용 한도를 끈 경우에도 같은 대화의 충돌과 요청 ID 중복 실행 방지는 유지합니다.

브라우저의 기기 자료 삭제는 서버의 AgentCore Memory 삭제와 별개입니다.
집계 값, 로그, 단기 이벤트와 추출된 기억의 보관 범위를 같은 것으로 설명하지 않습니다.
발견한 노출은 실제 권한과 영향을 확인하고 해당 비밀값을 다루는 운영 절차에 따라 처리합니다.
