---
inclusion: always
---

# Kiro CLI · __ATLAS_PROJECT__

Read and follow AGENTS.md, toolchain.json and .atlas-cli-workshop.json in this directory.
Work only in this participant workspace and preserve its account, region and names.
Use `source ./activate.sh`; keep dependency management and account-wide Transaction Search changes disabled.
The Warmup Runtime returns deterministic JSON and does not invoke a model.
Do not read credentials, .env files, keychains, provider tokens or production outputs.
Do not use trust-all-tools or change shared bootstrap, VPC/NAT or existing Runtime resources.
Local validation and synthesis do not prove cloud deployment.
Only perform an owned cloud step when the learner has requested that step.
