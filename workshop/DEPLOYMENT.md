# 워크숍 공개 배포 기록

## 2026-09-25, AgentCore CLI 역할 학습 참고

04장 하단에 핵심 명령 요약을 추가하고 참고 메뉴 마지막에
`AgentCore CLI 역할과 실행 흐름`을 게시했습니다. 명령별 목적과 결과,
주요 파일, 코딩 어시스턴트와 Runtime, CDK 및 보조 스크립트의 역할을 설명합니다.
완료한 명령을 복습 목적으로 다시 실행하도록 요구하지 않습니다.

| 항목 | 실제 결과 |
|---|---|
| 실행 소스 | PR #13, GitHub main `c6c4cec77897dc11a01787515117e5f7317e58f9`, 검증한 파일 67개 일치 |
| 게시 이미지 | `workshop-cli-learning-20260925T041356Z` |
| ECS | `jeju-3d:36`에서 `jeju-3d:37`, `UPDATE_COMPLETE`, 태스크 2/2와 ALB 대상 2개 정상 |
| 안정화 확인 | 2026-09-25 04:29:18 UTC, 이전 연결 정리 완료 |
| 변경 범위 | 기존 ImageUri 하나, 교재 경로 31개만 변경. 앱 파일과 이미지 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 갱신, 04:29:54 UTC 완료 확인 |
| 공개 확인 | 57개 파일과 교재 진입 경로 2개 일치, 기존 앱의 `/`, `/sw.js` 동일, healthz/readyz 정상 |
| 실제 브라우저 | 1366px/390px, 04장 하단 연결과 메뉴 마지막 위치, 명령 6개와 주요 경로 5개, 인쇄 및 오프라인 열람 확인 |
| 유지관리 검사 | 워크숍 269개와 전체 저장소 검사 통과, 과정 목록의 전체 페이지를 PWA 캐시에 포함 |

공개 ZIP은 27페이지, 883,028바이트이며 SHA-256은 다음과 같습니다.

```text
af634210d99a87cf0b50439db5fe95a900edc7395bb0c6b6d6ca37606856581f
```

참가자의 Runtime과 AWS 자원은 이 게시 작업에서 생성하거나 변경하지 않았습니다.
증거는 `workshop/.local/publication-cli-learning-20260925T041356Z/`에 보관했습니다.

## 2026-09-25, 서울 리전 CDK bootstrap 생성 단계 명시

사전 구성의 5절을 5-1 조회, 5-2 서울 리전에서 실제 생성, 5-3 완료 확인으로
구분했습니다. `missing/ready: false` 예시와 생성 명령을 연결하고 페이지 상단에
생성 단계 바로가기를 추가했습니다. 명령과 계정/자원 부재 확인 동작은 유지했습니다.

| 항목 | 실제 결과 |
|---|---|
| 실행 소스 | PR #11, GitHub main `491aeb7f31163d28eb3897cb102509b5223b8007`, 검증한 파일 61개 일치 |
| 게시 이미지 | `workshop-bootstrap-guide-20260925T034202Z` |
| ECS | `jeju-3d:35`에서 `jeju-3d:36`, `UPDATE_COMPLETE`, 태스크 2/2와 ALB 대상 2개 정상 |
| 안정화 확인 | 2026-09-25 03:52:15 UTC, 이전 연결 정리 완료 |
| 변경 범위 | 기존 ImageUri 하나, 교재 경로 29개만 변경. 앱 파일과 이미지 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 갱신, 03:52:46 UTC 완료 확인 |
| 공개 확인 | 56개 파일과 교재 진입 경로 2개 일치, 기존 앱의 `/`, `/sw.js` 동일, healthz/readyz 정상 |
| 실제 브라우저 | 1366px/390px, 생성 바로가기와 5-2/5-3 안내, 명령 20개 복사 및 오프라인 이동 확인 |
| 유지관리 검사 | 워크숍 269개와 전체 저장소 검사 통과 |

공개 ZIP은 26페이지, 867,900바이트이며 SHA-256은 다음과 같습니다.

```text
5d3625fcbf71f75729ef46a29886fd891373d81d5e645e26e90351554862514a
```

참가자 계정에서 bootstrap을 실행하거나 AWS 자원을 생성한 작업은 아닙니다.
공개 매뉴얼의 실행 안내를 보완한 결과이며, 실제 생성은 명시된 계정의 관리자 작업입니다.
증거는 `workshop/.local/publication-bootstrap-guide-20260925T034202Z/`에 보관했습니다.

## 2026-09-25, CDK 사전 준비와 단기·장기 API 키 지원

새 계정의 CDKToolkit과 기본 SSM 버전 파라미터를 읽기 전용으로 확인하도록
사전 구성과 04장을 보완했습니다. 처음 생성하는 계정의 진행자 절차와,
이미 만든 프로젝트의 `agentcore/cdk/node_modules/.bin/cdk`를 재사용하는 복구 경로를 안내합니다.
단기 또는 장기 Bedrock API 키를 같은 두 항목 입력과 Bearer 인증으로 사용합니다.

| 항목 | 실제 결과 |
|---|---|
| 실행 소스 | PR #9, GitHub main `42eede91bcb6e46a08f20010426392812d4b275b`, 검증한 파일 115개 일치 |
| 게시 이미지 | `workshop-bootstrap-keys-20260925T024207Z` |
| ECS | `jeju-3d:34`에서 `jeju-3d:35`, `UPDATE_COMPLETE`, 태스크 2/2와 ALB 대상 2개 정상 |
| 안정화 확인 | 2026-09-25 03:00:54 UTC, 이전 연결 정리 완료 |
| 변경 범위 | 기존 ImageUri 하나, 교재 경로 34개만 변경. 앱 파일과 이미지 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 갱신, 03:01:39 UTC 완료 확인 |
| 공개 파일 | 56개 파일과 교재 진입 경로 2개가 검증 이미지와 일치 |
| 앱 확인 | `/`, `/sw.js` 내용 동일, `/healthz`, `/readyz` 정상 |
| 실제 브라우저 | 1366px/390px, bootstrap 명령 3개를 포함한 명령 20개 복사, 두 항목 키 안내, 페이지 연결과 오프라인 이동, 오류/API 요청 없음 |
| 유지관리 검사 | 워크숍 269개와 전체 저장소 검사 통과 |
| CDK 명령 확인 | 설치된 CDK 2.1126.0의 `--show-template` 출력 버전 32, lint 오류 없음과 표준 템플릿 경고 2개 |

공개 ZIP은 26페이지, 866,339바이트이며 SHA-256은 다음과 같습니다.

```text
a0de8cc3a110fd34b7ec22f23dc455ef2a09e6185381324f1a1f189ed7b170ce
```

기존 소스는 갱신 후 같은 참가자 환경에서 사용합니다.
사용자가 bootstrap 완료를 확인한 환경은 재생성하지 않고 준비 상태 확인 후 04장을 이어갑니다.
보고된 다른 계정에 접근하거나 CDKToolkit과 Runtime을 생성하지 않았으며 실제 모델 호출도 수행하지 않았습니다.
증거는 `workshop/.local/publication-bootstrap-20260925T024207Z/`에 보관했습니다.

## 2026-09-25, 개요에서 설치와 키 입력으로 연결

00장은 목표와 준비 상태별 이동을 안내하고, 사전 구성은 설치와 점검을 담당하도록
연결했습니다. 사전 구성을 마치면 01장의 공통 환경 활성화와 키 입력으로 바로 이동합니다.
01장의 반복 설치 명령과 수동 키 만료 시각 입력을 제거했습니다.
키 입력은 발급 리전과 숨김 단기키 두 항목이며, 기존 `.env` 값은 보존합니다.

| 항목 | 실제 결과 |
|---|---|
| 실행 소스 | PR #7, GitHub main `3c90c0fcce78fa0fd9c7fdab969c0383944b9cbc`, 검증한 파일 85개 일치 |
| 게시 이미지 | `workshop-flow-20260925T014625Z` |
| ECS | `jeju-3d:33`에서 `jeju-3d:34`, `UPDATE_COMPLETE`, 태스크 2/2와 ALB 대상 2개 정상 |
| 안정화 확인 | 2026-09-25 02:06:55 UTC, 이전 연결 정리 완료 |
| 변경 범위 | 기존 ImageUri 하나, 교재 경로 33개만 변경. 앱 파일과 이미지 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 갱신, 02:07:55 UTC 완료 확인 |
| 공개 파일 | 56개 파일과 교재 진입 경로 2개가 검증 이미지와 일치 |
| 앱 확인 | `/`, `/sw.js` 내용 동일, `/healthz`, `/readyz` 정상 |
| 실제 브라우저 | 1366px/390px, 00장 분기와 사전 구성에서 01장 직접 이동, 명령 17개 복사, 오프라인 이동, 오류/API 요청 없음 |
| 키 입력 | 실제 가상 터미널에서 합성 값 두 항목 입력, 키 화면 출력 없음, `.env` 권한 0600 |
| 유지관리 검사 | 워크숍 232개와 전체 저장소 검사 통과 |

공개 ZIP은 26페이지, 861,799바이트이며 SHA-256은 다음과 같습니다.

```text
15c2be3656c7a7ce2f8283d2dc8b08972ac12ede2fc500bfe8897db4885111ae
```

기존 EC2는 소스를 갱신한 뒤 키 입력 명령을 다시 실행합니다.
기존 입력 프로세스가 만료 시각을 기다리고 있다면 종료 후 새 소스로 재실행합니다.
키 유효성은 실제 Bedrock 호출에서 판단하며 별도 모델 사전 호출을 추가하지 않았습니다.
이번 게시에서 참가자 Runtime을 배포하거나 실제 모델을 호출하지 않았습니다.
증거는 `workshop/.local/publication-flow-20260925T014625Z/`에 보관했습니다.

## 2026-09-25, 기존 실습 재개와 도구 선택 전환

같은 실습에서 Codex를 사용하다 Claude Code 또는 Kiro로 바꿀 때 발생하던
소유권 오류를 수정해 GitHub main에 반영했습니다. 생성된 도구 선택 정보만
갱신하며 프로젝트, `.env`, 계정과 CLI 설정을 보존합니다.
Node 전용 준비는 참가자 폴더와 분리하고 `toolchain/activate-node.sh`를 안내합니다.

00장에서 사전 구성으로 먼저 이동하도록 안내하고, 01장에도 세 도구의 시작 탭을
적용했습니다. 기존 clone의 소스 갱신과 같은 실습을 재개하는 절차도 추가했습니다.

| 항목 | 실제 결과 |
|---|---|
| 공개 교재 | 사전 구성, 00장과 01장, 연결된 프롬프트와 다운로드 ZIP 반영 |
| 실행 소스 | PR #5, GitHub main `82eefd20a727ccf5dad76f8209b5b45d519ef1b6`, 검증한 파일 72개 일치 |
| 게시 이미지 | `workshop-resume-20260925T004650Z` |
| ECS | `jeju-3d:32`에서 `jeju-3d:33`, `UPDATE_COMPLETE`, 태스크 2/2와 ALB 대상 2개 정상 |
| 안정화 확인 | 2026-09-25 01:06:13 UTC, 이전 ALB 연결 정리 완료 |
| 변경 범위 | 기존 ImageUri 하나, 교재 경로 31개만 변경. 앱 파일과 이미지 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 갱신, 01:07:21 UTC 완료 확인 |
| 공개 파일 | 56개 파일과 교재 진입 경로 2개가 검증 이미지와 일치 |
| 앱 확인 | `/`, `/sw.js` 내용 동일, `/healthz`, `/readyz` 정상 |
| 실제 브라우저 | 1366px/390px, 명령 18개 복사, 사전 구성과 01장 사이의 선택 유지, 오프라인 이동, 오류/API 요청 없음 |
| 유지관리 검사 | 워크숍 226개와 전체 저장소 검사 통과, 이전 Codex 세션에서 Node 설치 후 Claude 전환 확인 |

공개 ZIP은 26페이지, 863,986바이트이며 SHA-256은 다음과 같습니다.

```text
2bcbf16fe4b48a7ff0f7d2feb3a5c27fa0d39a31999c0e77b880c911e052e313
```

다른 EC2의 참가자는 기존 소스를 `git pull --ff-only origin main`으로 갱신한 뒤
같은 `start.sh --assistant claude`로 재개합니다. 이번 게시 작업에서 참가자
Runtime을 배포하거나 실제 모델을 호출하지 않았습니다.
증거는 `workshop/.local/publication-resume-20260925T004650Z/`에 보관했습니다.

## 2026-09-25, 도구별 명령 탭과 Node 24 설치 안내

사전 구성 페이지의 사전 점검, Node 설치, 도구 준비와 활성화 명령을
코덱스 / 클로드 코드 / 키로 탭으로 제공했습니다. 다섯 구간의 선택을 동기화하고,
선택한 명령만 복사합니다. 키보드 전환, 선택 유지, 인쇄와 JavaScript 없는 열람도 지원합니다.

Node 20 환경에는 `start.sh --node-only`를 이용한 Node 24 설치 절차를 추가했습니다.
Node 20과 시스템 Python 경로에서 uv 없이 Node 24.21.0 설치와 npm 활성화를 직접 확인했습니다.
이 확인은 참가자 Runtime 배포나 실제 모델 호출 검증을 포함하지 않습니다.

| 항목 | 실제 결과 |
|---|---|
| 공개 페이지 | `https://jeju-atlas.whchoi.net/workshop/reference/preconfiguration.html` |
| 게시 이미지 | `workshop-command-tabs-20260924T235511Z` |
| ECS | `jeju-3d:31`에서 `jeju-3d:32`, `UPDATE_COMPLETE`, 태스크 2/2와 ALB 대상 2개 정상 |
| 이미지 대조 | 교재 경로 31개만 변경, 앱 파일과 실행 설정 동일 |
| 공개 파일 | 56개 파일과 두 교재 진입 경로가 검증 이미지와 일치 |
| 실제 브라우저 | 1366px/390px, 각 화면에서 명령 15개 복사, 탭 5구간 동기화, 키보드/선택 유지/오프라인 동작, 오류/API 요청 없음 |
| 실행 소스 | PR #3, GitHub main `56caea7e9f767c76a35940450acc5a249c9a1299` |
| 유지관리 검사 | 전체 저장소 검사, 워크숍 219개 검사, Node 설치 및 탭 회귀 검사 통과 |

공개 ZIP의 SHA-256:

```text
3b35e94586eabd6eb31dc235160e786d4509d06a60402ccc9c500f74f900a6f8
```

배포와 브라우저, 소스 대조 증거는 `workshop/.local/publication-tabs-20260924T235511Z/`에 보관했습니다.

## 2026-09-24, 독립 EC2 랩과 간소화한 실행 흐름

EC2 사전 준비, 소스 clone과 실행 위치, 단기키 입력을 정리한 교재를 공개했습니다.
참가자별 독립 랩은 공통 실습 ID `team01`을 자동 사용하며 팀명을 바꾸지 않습니다.
터미널 블록 93개는 올바른 `cd`로 시작합니다. 종합 검증과 HUD는 선택 사항이며,
기본 흐름은 구현, 배포와 예시 응답 1회입니다. 문장 줄바꿈과 용어도 통일했습니다.

| 항목 | 실제 결과 |
|---|---|
| 공개 경로 | `https://jeju-atlas.whchoi.net/workshop/` |
| 게시 이미지 태그 | `workshop-ec2-20260924T224131Z` |
| ECS 태스크 정의 | `jeju-3d:30`에서 `jeju-3d:31`로 갱신 |
| 서비스 안정화 | 2026-09-24 23:07 UTC, `UPDATE_COMPLETE`, 태스크 2/2, ALB 대상 2개 정상 |
| 변경 세트 | `ImageUri` 하나와 기존 TaskDefinition/Service의 Modify |
| 이미지 대조 | `/app/dist/workshop/` 아래 47개 경로만 변경, 앱 파일과 실행 설정 동일 |
| 공개 파일 | 56개 파일이 검증 이미지와 SHA-256 일치, 두 교재 진입 경로 정상 |
| 앱 확인 | `/`, `/sw.js` 내용 동일, `/healthz`, `/readyz` 정상 |
| 실제 브라우저 | 1366px/390px, 터미널 9개 복사, 세 어시스턴트 프롬프트 복사, 오프라인 3페이지, 오류/API 요청 없음 |
| 실행 소스 | GitHub main에 PR #1 병합, `191ad6f8eab37a469bae0ee30f23956656550f33` |
| 교재 | 26페이지, 다운로드 ZIP 55파일 |

공개 ZIP의 SHA-256은 다음과 같습니다.

```text
06059652eaea52f0bfb11f8ba0be94bccf4b15e2165fc355a514291d1d9fc074
```

배포 전에 전체 저장소 검사와 워크숍 검사 202개, 게시 경계 검사 6개를 확인했습니다.
원격 main의 `start.sh`, `.env` 입력 도구와 스킬이 로컬 검증본과 일치합니다.
참가자의 AWS 배포와 유료 모델 호출은 게시 검증에 포함하지 않았습니다.
증거는 `workshop/.local/publication-20260924T224131Z/`에 보관했습니다.

## 2026-09-16, 도구 사전 구성과 PyYAML 복구

Node 24, Python 3.14, Python 보조 패키지와 npm AgentCore CLI 설치를 사전 구성으로
정리했습니다. 전체 Atlas 작업은 같은 helper Python에 PyYAML과 cfn-lint를 준비한 뒤
앱 사본을 만들도록 했습니다. 01장은 설치 결과 확인, 14장은 미완료 사전 구성을 먼저
끝내고 구현을 이어 가는 흐름입니다. 본 실습 120분은 유지했습니다.

| 항목 | 실제 결과 |
|---|---|
| 공개 안내 | `https://jeju-atlas.whchoi.net/workshop/reference/preconfiguration.html` |
| 게시 이미지 태그 | `workshop-preconfiguration-20260916T044905Z` |
| ECS 태스크 정의 | `jeju-3d:29`에서 `jeju-3d:30`로 변경 |
| 안정화 확인 | 2026-09-16 04:55:40 UTC, `UPDATE_COMPLETE`, 태스크 2/2, ALB 대상 2개 정상 |
| 변경 세트 | 기존 web 이미지와 Service의 TaskDefinition 참조, 매개변수는 ImageUri 하나 |
| 이미지 파일 대조 | 변경 경로 32개가 모두 `/app/dist/workshop/` 아래, 나머지 파일과 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 무효화, 04:56:59 UTC 완료 |
| 공개 파일 | 55개 파일이 검증한 이미지와 SHA-256 일치, 두 교재 진입 경로 정상 |
| 앱 확인 | `/`, `/sw.js` 내용 유지, `/healthz`, `/readyz` 정상 |
| 실제 브라우저 | 1366px/390px에서 사전 구성 링크, 설치 명령 6개와 14장 전체 프롬프트 복사 확인 |
| 오프라인 | 05장, 사전 구성 안내와 14장을 워크숍 서비스 워커에서 열람 |
| 교재 | 25페이지, ZIP 54파일, 821,720바이트 |

게시 ZIP의 SHA-256은 다음과 같습니다.

```text
3dae9379909a2577bc2bda9a84528ce419ee4988263096189caf2d31474157bb
```

교재 검사 137개와 공개 교재/PWA 검사 10개를 통과했습니다.
PyYAML 누락 시 부분 사본이 남는 상황을 먼저 실패 테스트로 확인하고,
준비기 원본에 복사 전 의존성 검사를 추가해 관련 경계 검사 7개를 통과했습니다.
이 Python 원본 변경은 로컬 소스 변경이며 교재 이미지 배포나 Git push를 통해
참가자의 기존 원본이 갱신됐다는 의미는 아닙니다.

이전 공개 원본에서도 PyYAML 누락을 모의 재현했습니다. 부분 사본의 사용자 메모를
백업으로 보존한 뒤, yaml import가 가능한 환경에서 같은 참가자와 config로 재준비했습니다.
이 검증은 로컬 파일 처리 재현이며 실제 다른 AWS 계정에서 수행한 결과는 아닙니다.
현재 프롬프트는 원본 버전에 관계없이 같은 Python의 사전 import 검사와 복구 절차를 사용합니다.

검증 환경의 Node는 24.21.0, Python은 3.14.3이며 boto3 1.42.86,
requests 2.32.5, PyYAML 6.0.3, cfn-lint 1.45.0을 확인했습니다.
이번 작업에서 참가자 EC2의 실제 도구 설치나 모델 호출을 수행하지 않았습니다.

기준 소스는 `04e12b9ac29732de90ec0bf7a591fb08d1a56584`와 당시 미커밋 변경입니다.
소스 및 검사 파일 58개 해시, 이미지와 공개 검증은
`workshop/.local/preconfiguration/publication/`에 보관했습니다.
기존 앱 릴리스, 라우터, 네트워크, 도메인, IAM과 AgentCore 설정은 유지했습니다.

## 2026-09-16, 새 계정 초기화 프롬프트와 Codex 설정

14장 프롬프트가 소스와 참가자 설정이 없는 다른 계정에서도 준비 작업부터 시작하도록
수정했습니다. 기본 참가자 유형과 Git 저장소 URL을 입력 블록에 포함하고,
참가자 이름, 실제 경로와 JSON 설정을 준비 과정의 결과로 기록하도록 했습니다.
Codex 설정 원문과 읽기 전용 검사, Python 파일이 없을 때의 실행 소스 안내도 함께 게시했습니다.

| 항목 | 실제 결과 |
|---|---|
| 공개 프롬프트 | `https://jeju-atlas.whchoi.net/workshop/prompts/14-project-completion.md` |
| Codex 설정 안내 | `https://jeju-atlas.whchoi.net/workshop/reference/codex-bedrock.html` |
| 게시 이미지 태그 | `workshop-bootstrap-20260916T022633Z` |
| ECS 태스크 정의 | `jeju-3d:28`에서 `jeju-3d:29`로 변경 |
| 안정화 확인 | 2026-09-16 02:34:41 UTC, `UPDATE_COMPLETE`, 태스크 2/2, ALB 대상 2개 정상 |
| 변경 세트 | 기존 web 이미지와 Service의 TaskDefinition 참조, 매개변수는 ImageUri 하나 |
| 이미지 파일 대조 | 변경 경로 30개가 모두 `/app/dist/workshop/` 아래, 나머지 파일과 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 무효화, 02:37:42 UTC 완료 |
| 공개 파일 | 54개 파일이 검증한 이미지와 SHA-256 일치, 두 교재 진입 경로 정상 |
| 앱 확인 | `/`, `/sw.js` 내용 유지, `/healthz`, `/readyz` 정상 |
| 실제 브라우저 | 1366px/390px에서 실행 소스 링크, 설정 링크, 원문 TOML과 14장 전체 프롬프트 복사 확인 |
| 오프라인 | 13장, Codex 설정 안내와 14장을 워크숍 서비스 워커에서 열람 |
| 교재 | 24페이지, ZIP 53파일, 803,625바이트 |

게시 ZIP의 SHA-256은 다음과 같습니다.

```text
774359cc901386aafb0827cc99dee920902670047b94bb7b9f7c4d5ab80487cd
```

교재 검사 136개와 공개 교재/PWA 검사 10개를 통과했습니다.
제공된 TOML 원문을 보존했으며 예제 검사에서 정상 값은 통과하고
잘못된 테이블 위치와 리전은 거부했습니다. Codex CLI 0.154.0의 읽기 전용
설정 파싱도 확인했으며 사용자 설정 파일 변경이나 실제 모델 요청은 하지 않았습니다.

초기화 재현은 기존 메모가 있는 빈 작업 폴더에서 시작했습니다.
새 소스 확보, 참가자 이름과 config 생성, 앱 사본 준비, 새 셸 복원과 재개를 확인했습니다.
이 재현의 EC2 신원, STS 경계와 네트워크는 합성 fixture로 모의 처리했습니다.
**실제 다른 AWS 계정에서 프롬프트를 실행한 검증은 아닙니다.**

별도로 공개 Git 저장소를 새 clone하여 `workshop/scripts/`의 Python 파일 12개와
`core.py source` 통과를 확인했습니다. 읽기용 교재 ZIP은 계속 실행 스크립트를 제외하며,
첫 화면과 다운로드 안내에서 전체 Git 소스 준비로 연결합니다.

기준 소스는 `04e12b9ac29732de90ec0bf7a591fb08d1a56584`와 당시 미커밋 교재 변경입니다.
소스 파일 55개 해시, 이미지, 변경 세트와 공개 검증은
`workshop/.local/codex-bedrock-guide/publication/`에 보관했습니다.
기존 앱 릴리스, 라우터, 네트워크, 도메인, IAM과 AgentCore 설정은 유지했습니다.
참가자 Runtime 배포, 실제 모델 호출과 Git push는 이번 게시에 포함하지 않았습니다.

## 2026-09-16, HUD 사전 설치 안내

Codex용 my-codex-hud 사전 설치 안내를 01장, 진행자 가이드와 Agentic AI 코딩 어시스턴트 안내에 연결했습니다.
기존 14장과 함께 공개 교재에 반영했으며 본 실습 120분은 유지합니다.
진행자가 수업 전에 프로젝트 전용 경로에 설치하고 버전, 진단과 실제 TTY를 확인하는 절차입니다.

| 항목 | 실제 결과 |
|---|---|
| 공개 안내 | `https://jeju-atlas.whchoi.net/workshop/reference/hud-setup.html` |
| 게시 이미지 태그 | `workshop-hud-20260916T012702Z` |
| ECS 태스크 정의 | `jeju-3d:27`에서 `jeju-3d:28`로 변경 |
| 안정화 확인 | 2026-09-16 01:33:37 UTC, `UPDATE_COMPLETE`, 태스크 2/2, ALB 대상 2개 정상 |
| 변경 세트 | 기존 web 이미지와 Service의 TaskDefinition 참조, 매개변수는 ImageUri 하나 |
| 이미지 파일 대조 | 변경 경로 28개가 모두 `/app/dist/workshop/` 아래, 나머지 파일과 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 무효화, 01:35:25 UTC 완료 |
| 공개 파일 | 53개 파일이 검증한 이미지와 SHA-256 일치, 두 교재 진입 경로 정상 |
| 앱 확인 | `/`, `/sw.js` 내용 유지, `/healthz`, `/readyz` 정상 |
| 실제 브라우저 | 1366px/390px에서 01장 링크, HUD 설치 프롬프트와 14장 복사, 본문 넘침과 오류 없음 |
| 오프라인 | 13장, 14장과 HUD 안내를 `/workshop/` 서비스 워커에서 열람, API 요청 없음 |
| 교재 | 23페이지, ZIP 52파일, 783,309바이트 |

게시 ZIP의 SHA-256은 다음과 같습니다.

```text
c542070cdea110998587319bdd8ea3645457f1fd10ce93193e8159419207dd25
```

교재 검사 136개와 공개 교재/PWA 검사 10개를 통과했습니다.
공개 my-codex-hud 소스 `bf1e6489345a3ff7ee8a20ad23d56c0657f53516`의
0.7.0 번들과 체크섬을 대조하고 설치 도구를 dry-run으로 확인했습니다.
계획의 scope는 project, shell은 none, autostart는 false이며 startupFiles는 빈 목록입니다.
이 검증에서는 HUD를 실제 설치하거나 셸 시작 파일을 변경하지 않았습니다.
참가자 EC2의 실제 설치와 TTY 확인은 가이드에 따라 진행자가 별도로 수행합니다.

기준 소스는 `04e12b9ac29732de90ec0bf7a591fb08d1a56584`와 당시 미커밋 교재 변경입니다.
소스 파일 54개의 해시, 이미지, 변경 세트, 공개 파일과 브라우저 검증은
`workshop/.local/hud-guide/publication/`에 보관했습니다.
기존 앱 릴리스, 라우터, 네트워크, 도메인, IAM과 AgentCore 설정은 유지했습니다.
실제 모델 호출, 참가자 Runtime 리허설과 Git push는 이번 게시에 포함하지 않았습니다.

## 2026-09-16, 프로젝트 완성 프롬프트와 14장

Codex 통합 프롬프트와 14장을 기존 서비스의 교재 경로에 게시했습니다.
AgentCore CLI, Guide/Tools/Gateway/Memory, CloudFront/ALB/Fargate, S3와
DynamoDB의 구현 및 검증 기준을 포함합니다. 본 실습 120분은 유지했습니다.

| 항목 | 실제 결과 |
|---|---|
| 공개 장 | `https://jeju-atlas.whchoi.net/workshop/chapters/14-project-completion.html` |
| 게시 이미지 태그 | `workshop-chapter14-20260916T005635Z` |
| ECS 태스크 정의 | `jeju-3d:26`에서 `jeju-3d:27`로 변경 |
| 안정화 확인 | 2026-09-16 01:06:41 UTC, `UPDATE_COMPLETE`, 태스크 2/2, ALB 대상 2개 정상 |
| 변경 세트 | 기존 web 이미지와 Service의 TaskDefinition 참조, 매개변수는 ImageUri 하나 |
| 이미지 파일 대조 | 변경 경로 60개가 모두 `/app/dist/workshop/` 아래, 나머지 파일과 실행 설정 동일 |
| CloudFront | `/workshop`, `/workshop/*` 무효화, 01:07:50 UTC 완료 |
| 공개 파일 | 52개 파일이 검증한 이미지와 SHA-256 일치, 두 교재 진입 경로 정상 |
| 앱 확인 | `/`, `/sw.js` 내용 유지, `/healthz`, `/readyz` 정상 |
| 실제 브라우저 | 1366px/390px, 세 도구의 전체 프롬프트 복사, 오프라인 14장, 범위 `/workshop/`, 오류 및 API 요청 없음 |
| 교재 | 22페이지, ZIP 51파일, 770,151바이트 |

게시 ZIP의 SHA-256은 다음과 같습니다.

```text
0f72a500a996f403bcc995cbe6aba4f23720062320ddab05bed0753f48bc8448
```

배포 전 교재 검사 136개, 공개 교재/PWA 검사 10개와 게시 도구 검사 6개를 확인했습니다.
이미지의 앱 릴리스 값과 라우터, 네트워크, 도메인, IAM과 AgentCore 구성은 유지했습니다.
기준 소스는 `04e12b9ac29732de90ec0bf7a591fb08d1a56584`와 당시 미커밋 교재 변경입니다.
소스 파일 53개의 해시, 이전 이미지, 변경 세트와 공개 검증 결과는
`workshop/.local/chapter14/publication/`에 보관했습니다.
실제 모델 호출, 참가자 Runtime 리허설과 Git push는 이번 게시에 포함하지 않았습니다.

## 2026-09-12, 120분 교재와 화면 개선

2026-09-12에 120분 교재와 본문 화면 개선을 운영 워크숍 경로에 배포했습니다.

### 반영 내용

- 본 실습 5개 장은 110분이며 여유 10분을 더해 120분으로 편성했습니다.
- 나머지 9개 장은 선택하는 심화 과정으로 표시합니다.
- 1366px 화면의 본문 폭은 기존 760px에서 약 1020px로 넓어졌습니다.
- 1920px 화면의 본문 폭은 1268px입니다.
- 노트북 화면에서는 페이지 목차를 본문 위에 접힌 형태로 표시합니다.
- 셸 명령은 맥북 터미널 형태의 창으로 표시합니다.
- AI 프롬프트는 Codex, Kiro CLI, Claude Code 선택에 따라 이름과 색상을 구분합니다.
- 명령은 터미널로, 프롬프트는 선택한 Agentic AI 코딩 어시스턴트의 대화 입력창으로 복사하도록 표시합니다.
- 복사 내용에는 창 장식이나 교재의 부연 설명이 포함되지 않습니다.

### 배포 상태

| 항목 | 결과 |
|---|---|
| 공개 경로 | `https://jeju-atlas.whchoi.net/workshop/` |
| 릴리스 | `release-20260912T153512Z` |
| ECS 태스크 정의 | `jeju-3d:26` |
| 완료 확인 | 2026-09-12 15:42:59 UTC |
| 서비스 상태 | `UPDATE_COMPLETE`, 태스크 2/2, ALB 대상 2개 정상 |
| 변경 범위 | 앱 이미지와 릴리스 번호 |
| CloudFront 갱신 | `/workshop*`, `IBN3CH839J07QP1PJ85BQ8P6WZ`, Completed |
| 이미지 검사 | 검사 완료 시각 확인, HIGH/CRITICAL 0개 |

기존 네트워크와 AgentCore, AI 한도 해제, 접속 집계 설정은 유지했습니다.
원본 작업 폴더의 다른 작업과 겹치지 않도록 별도 복사본에서 빌드했습니다.
원본 저장소에서 스테이징이나 커밋을 수행하지 않았습니다.

### 확인 결과

워크숍 검사 41개를 실제 브라우저 검사를 포함해 통과했습니다.
앱 배포 검사와 운영 검사 99개도 통과했습니다.
공개 페이지는 1920, 1440, 1366, 1280, 1024, 390px 화면에서 확인했습니다.
터미널 명령 복사, AI 도구 선택과 프롬프트 복사, 선택 기억이 정상 동작했습니다.

공개 PWA 검사 5개에서 설치 범위, ZIP 다운로드, 체크섬,
오프라인 교재와 지도 앱의 캐시 분리를 확인했습니다.
공개 다운로드 ZIP의 SHA-256은 다음과 같습니다.

```text
43517d815c51dabf03637c3e787f5f92552fced738c75082ec1c0ec0a12f879a
```

검증 자료와 배포용 소스 복사본은 `workshop/.local/deploy-120-wide/`에 있습니다.
이번 배포는 교재 서비스의 배포입니다.
참가자용 에이전트를 120분 동안 구현하고 배포하는 클라우드 리허설은 별도입니다.
